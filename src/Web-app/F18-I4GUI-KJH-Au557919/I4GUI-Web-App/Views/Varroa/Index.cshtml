@model IEnumerable<I4GUIWebApp.Models.VarroaCount>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Beehive)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.DOC)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Varroa)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ObservationLength)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Comments)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Beehive)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DOC)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Varroa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ObservationLength)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Comments)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
